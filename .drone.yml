kind: pipeline
name: default

steps:
- name: test
  image: chg80333/goadmin-test
  commands:
  - sleep 500
  - mysql -u root -h database --execute="SELECT VERSION();"

services:
- name: database
  image: mysql:5.7
  environment:
    MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
    MYSQL_DATABASE: test